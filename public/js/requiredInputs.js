document.addEventListener("DOMContentLoaded", function(event) {
    /*

    var titleRequired = document.getElementById("title_BE");
    var titleWithoutPrefixRequired = document.getElementById("titleWP_BE");

    console.log(titleRequired);

    if (titleRequired != null) {
        titleWithoutPrefixRequired.setAttribute("style", "class: form-control requiredInput;");
    }
    */
    /*
    if (titleRequired == null) {
        titleWithoutPrefixRequired.removeAttribute("class");
        titleWithoutPrefixRequired.setAttribute("style", "class: form-control");
    }
    */
});
